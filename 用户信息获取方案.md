# 用户信息获取方案 - 取消注册登录后

## 📋 概述

当取消注册登录页面，用户进入 APP 时默认分配一个账户。本文档详细说明在 React Native/Expo 环境下，前端可以获取到的用户信息。

---

## 🤖 Android 系统

### 1. 设备标识信息

#### ✅ 可以获取的信息

| 信息类型 | Expo API | 说明 | 隐私级别 |
|---------|----------|------|---------|
| **设备 ID** | `expo-application` | 应用安装实例 ID（每次安装唯一） | 低 |
| **设备型号** | `expo-device` | 如 "Pixel 5", "Samsung Galaxy S21" | 低 |
| **设备品牌** | `expo-device` | 如 "Google", "Samsung" | 低 |
| **操作系统版本** | `expo-device` | 如 "Android 13" | 低 |
| **应用版本** | `expo-application` | 当前应用版本号 | 低 |
| **设备名称** | `expo-device` | 用户设置的设备名称 | 中 |

#### 📦 需要安装的包

```bash
npx expo install expo-device expo-application
```

#### 💻 代码示例

```javascript
import * as Device from 'expo-device';
import * as Application from 'expo-application';

// 获取设备信息
const deviceInfo = {
  deviceId: Application.androidId, // Android ID（应用级别唯一）
  deviceName: Device.deviceName,   // 设备名称
  brand: Device.brand,             // 品牌：如 "google"
  manufacturer: Device.manufacturer, // 制造商：如 "Google"
  modelName: Device.modelName,     // 型号：如 "Pixel 5"
  osName: Device.osName,           // "Android"
  osVersion: Device.osVersion,     // "13"
  platformApiLevel: Device.platformApiLevel, // API 级别：如 33
  deviceType: Device.deviceType,   // 1=手机, 2=平板, 3=桌面, 4=电视
  appVersion: Application.nativeApplicationVersion,
  buildNumber: Application.nativeBuildVersion,
};
```

### 2. 位置信息

#### ✅ 可以获取的信息（需要权限）

| 信息类型 | Expo API | 说明 | 需要权限 |
|---------|----------|------|---------|
| **GPS 位置** | `expo-location` | 经纬度坐标 | ✅ 是 |
| **地理编码** | `expo-location` | 地址信息（国家、城市、街道） | ✅ 是 |

#### 📦 需要安装的包

```bash
npx expo install expo-location
```

#### 💻 代码示例

```javascript
import * as Location from 'expo-location';

// 请求位置权限并获取位置
async function getLocation() {
  // 请求权限
  const { status } = await Location.requestForegroundPermissionsAsync();
  
  if (status !== 'granted') {
    console.log('位置权限被拒绝');
    return null;
  }

  // 获取当前位置
  const location = await Location.getCurrentPositionAsync({
    accuracy: Location.Accuracy.Balanced,
  });

  // 反向地理编码（获取地址）
  const address = await Location.reverseGeocodeAsync({
    latitude: location.coords.latitude,
    longitude: location.coords.longitude,
  });

  return {
    latitude: location.coords.latitude,
    longitude: location.coords.longitude,
    city: address[0]?.city,
    region: address[0]?.region,
    country: address[0]?.country,
    postalCode: address[0]?.postalCode,
  };
}
```

### 3. 网络信息

#### ✅ 可以获取的信息

| 信息类型 | Expo API | 说明 | 需要权限 |
|---------|----------|------|---------|
| **网络状态** | `expo-network` | WiFi/蜂窝/无网络 | ❌ 否 |
| **IP 地址** | `expo-network` | 本地 IP 地址 | ❌ 否 |

#### 📦 需要安装的包

```bash
npx expo install expo-network
```

#### 💻 代码示例

```javascript
import * as Network from 'expo-network';

async function getNetworkInfo() {
  const networkState = await Network.getNetworkStateAsync();
  const ipAddress = await Network.getIpAddressAsync();

  return {
    isConnected: networkState.isConnected,
    isInternetReachable: networkState.isInternetReachable,
    type: networkState.type, // WIFI, CELLULAR, NONE, etc.
    ipAddress: ipAddress,
  };
}
```

### 4. 语言和区域设置

#### ✅ 可以获取的信息

| 信息类型 | Expo API | 说明 | 需要权限 |
|---------|----------|------|---------|
| **系统语言** | `expo-localization` | 如 "zh-CN", "en-US" | ❌ 否 |
| **时区** | `expo-localization` | 如 "Asia/Shanghai" | ❌ 否 |
| **货币** | `expo-localization` | 如 "CNY", "USD" | ❌ 否 |

#### 📦 需要安装的包

```bash
npx expo install expo-localization
```

#### 💻 代码示例

```javascript
import * as Localization from 'expo-localization';

const localeInfo = {
  locale: Localization.locale,           // "zh-CN"
  locales: Localization.locales,         // ["zh-CN", "en-US"]
  timezone: Localization.timezone,       // "Asia/Shanghai"
  region: Localization.region,           // "CN"
  currency: Localization.currency,       // "CNY"
  isRTL: Localization.isRTL,            // false (是否从右到左)
};
```

### 5. 存储信息

#### ✅ 可以获取的信息

| 信息类型 | API | 说明 | 需要权限 |
|---------|-----|------|---------|
| **本地存储** | `AsyncStorage` | 持久化存储用户数据 | ❌ 否 |
| **SecureStore** | `expo-secure-store` | 加密存储敏感信息 | ❌ 否 |

#### 📦 需要安装的包

```bash
npx expo install @react-native-async-storage/async-storage expo-secure-store
```

#### 💻 代码示例

```javascript
import AsyncStorage from '@react-native-async-storage/async-storage';
import * as SecureStore from 'expo-secure-store';

// 生成或获取用户 ID
async function getOrCreateUserId() {
  let userId = await AsyncStorage.getItem('userId');
  
  if (!userId) {
    // 生成新的用户 ID
    userId = `user_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`;
    await AsyncStorage.setItem('userId', userId);
  }
  
  return userId;
}

// 存储用户 token（加密）
async function saveUserToken(token) {
  await SecureStore.setItemAsync('userToken', token);
}
```

### ❌ 无法获取的信息（Android）

| 信息类型 | 说明 | 原因 |
|---------|------|------|
| **手机号码** | 用户的手机号 | 需要 READ_PHONE_STATE 权限，且不可靠 |
| **IMEI** | 设备唯一标识 | Android 10+ 已禁止访问 |
| **MAC 地址** | 网络硬件地址 | Android 6+ 已限制访问 |
| **联系人** | 通讯录信息 | 需要 READ_CONTACTS 权限 |
| **短信** | 短信内容 | 需要 READ_SMS 权限 |
| **通话记录** | 通话历史 | 需要 READ_CALL_LOG 权限 |
| **已安装应用列表** | 其他应用信息 | Android 11+ 已限制 |

---

## 🍎 iOS 系统

### 1. 设备标识信息

#### ✅ 可以获取的信息

| 信息类型 | Expo API | 说明 | 隐私级别 |
|---------|----------|------|---------|
| **设备 ID** | `expo-application` | 应用安装实例 ID（IDFV） | 低 |
| **设备型号** | `expo-device` | 如 "iPhone 14 Pro" | 低 |
| **设备品牌** | `expo-device` | "Apple" | 低 |
| **操作系统版本** | `expo-device` | 如 "iOS 17.2" | 低 |
| **应用版本** | `expo-application` | 当前应用版本号 | 低 |
| **设备名称** | `expo-device` | 用户设置的设备名称 | 中 |

#### 📦 需要安装的包

```bash
npx expo install expo-device expo-application
```

#### 💻 代码示例

```javascript
import * as Device from 'expo-device';
import * as Application from 'expo-application';

// 获取设备信息
const deviceInfo = {
  deviceId: Application.getIosIdForVendorAsync(), // IDFV（同一开发者的应用共享）
  deviceName: Device.deviceName,   // 设备名称：如 "张三的 iPhone"
  brand: Device.brand,             // "Apple"
  manufacturer: Device.manufacturer, // "Apple"
  modelName: Device.modelName,     // "iPhone 14 Pro"
  osName: Device.osName,           // "iOS"
  osVersion: Device.osVersion,     // "17.2"
  deviceType: Device.deviceType,   // 1=手机, 2=平板, 3=桌面
  appVersion: Application.nativeApplicationVersion,
  buildNumber: Application.nativeBuildVersion,
};
```

### 2. 位置信息

#### ✅ 可以获取的信息（需要权限）

| 信息类型 | Expo API | 说明 | 需要权限 |
|---------|----------|------|---------|
| **GPS 位置** | `expo-location` | 经纬度坐标 | ✅ 是 |
| **地理编码** | `expo-location` | 地址信息（国家、城市、街道） | ✅ 是 |

#### 📦 需要安装的包

```bash
npx expo install expo-location
```

#### 💻 代码示例

```javascript
import * as Location from 'expo-location';

// 请求位置权限并获取位置
async function getLocation() {
  // iOS 需要在 app.json 中配置权限说明
  const { status } = await Location.requestForegroundPermissionsAsync();
  
  if (status !== 'granted') {
    console.log('位置权限被拒绝');
    return null;
  }

  const location = await Location.getCurrentPositionAsync({
    accuracy: Location.Accuracy.Balanced,
  });

  const address = await Location.reverseGeocodeAsync({
    latitude: location.coords.latitude,
    longitude: location.coords.longitude,
  });

  return {
    latitude: location.coords.latitude,
    longitude: location.coords.longitude,
    city: address[0]?.city,
    region: address[0]?.region,
    country: address[0]?.country,
    postalCode: address[0]?.postalCode,
  };
}
```

### 3. 网络信息

#### ✅ 可以获取的信息

| 信息类型 | Expo API | 说明 | 需要权限 |
|---------|----------|------|---------|
| **网络状态** | `expo-network` | WiFi/蜂窝/无网络 | ❌ 否 |
| **IP 地址** | `expo-network` | 本地 IP 地址 | ❌ 否 |

#### 📦 需要安装的包

```bash
npx expo install expo-network
```

#### 💻 代码示例

```javascript
import * as Network from 'expo-network';

async function getNetworkInfo() {
  const networkState = await Network.getNetworkStateAsync();
  const ipAddress = await Network.getIpAddressAsync();

  return {
    isConnected: networkState.isConnected,
    isInternetReachable: networkState.isInternetReachable,
    type: networkState.type, // WIFI, CELLULAR, NONE, etc.
    ipAddress: ipAddress,
  };
}
```

### 4. 语言和区域设置

#### ✅ 可以获取的信息

| 信息类型 | Expo API | 说明 | 需要权限 |
|---------|----------|------|---------|
| **系统语言** | `expo-localization` | 如 "zh-CN", "en-US" | ❌ 否 |
| **时区** | `expo-localization` | 如 "Asia/Shanghai" | ❌ 否 |
| **货币** | `expo-localization` | 如 "CNY", "USD" | ❌ 否 |

#### 📦 需要安装的包

```bash
npx expo install expo-localization
```

#### 💻 代码示例

```javascript
import * as Localization from 'expo-localization';

const localeInfo = {
  locale: Localization.locale,           // "zh-CN"
  locales: Localization.locales,         // ["zh-CN", "en-US"]
  timezone: Localization.timezone,       // "Asia/Shanghai"
  region: Localization.region,           // "CN"
  currency: Localization.currency,       // "CNY"
  isRTL: Localization.isRTL,            // false
};
```

### 5. 存储信息

#### ✅ 可以获取的信息

| 信息类型 | API | 说明 | 需要权限 |
|---------|-----|------|---------|
| **本地存储** | `AsyncStorage` | 持久化存储用户数据 | ❌ 否 |
| **SecureStore** | `expo-secure-store` | 加密存储（使用 Keychain） | ❌ 否 |

#### 📦 需要安装的包

```bash
npx expo install @react-native-async-storage/async-storage expo-secure-store
```

#### 💻 代码示例

```javascript
import AsyncStorage from '@react-native-async-storage/async-storage';
import * as SecureStore from 'expo-secure-store';

// 生成或获取用户 ID
async function getOrCreateUserId() {
  let userId = await AsyncStorage.getItem('userId');
  
  if (!userId) {
    userId = `user_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`;
    await AsyncStorage.setItem('userId', userId);
  }
  
  return userId;
}

// 存储用户 token（使用 iOS Keychain）
async function saveUserToken(token) {
  await SecureStore.setItemAsync('userToken', token);
}
```

### ❌ 无法获取的信息（iOS）

| 信息类型 | 说明 | 原因 |
|---------|------|------|
| **IDFA** | 广告标识符 | 需要 ATT 权限弹窗，用户可拒绝 |
| **UDID** | 设备唯一标识 | iOS 5+ 已禁止访问 |
| **手机号码** | 用户的手机号 | iOS 不提供 API |
| **联系人** | 通讯录信息 | 需要 Contacts 权限 |
| **照片** | 相册内容 | 需要 Photos 权限 |
| **日历** | 日历事件 | 需要 Calendar 权限 |
| **健康数据** | HealthKit 数据 | 需要 HealthKit 权限 |

---

## 🎯 推荐的用户识别方案

### 方案一：设备 ID + 本地存储（推荐）

```javascript
import * as Device from 'expo-device';
import * as Application from 'expo-application';
import AsyncStorage from '@react-native-async-storage/async-storage';
import { Platform } from 'react-native';

async function initializeUser() {
  // 1. 尝试从本地存储获取用户 ID
  let userId = await AsyncStorage.getItem('userId');
  
  if (!userId) {
    // 2. 生成新的用户 ID
    const timestamp = Date.now();
    const random = Math.random().toString(36).substr(2, 9);
    
    // 3. 获取设备信息作为辅助标识
    let deviceId;
    if (Platform.OS === 'android') {
      deviceId = Application.androidId;
    } else {
      deviceId = await Application.getIosIdForVendorAsync();
    }
    
    userId = `${Platform.OS}_${deviceId}_${timestamp}_${random}`;
    
    // 4. 保存到本地
    await AsyncStorage.setItem('userId', userId);
  }
  
  // 5. 收集设备信息
  const userProfile = {
    userId: userId,
    platform: Platform.OS,
    deviceName: Device.deviceName,
    deviceModel: Device.modelName,
    osVersion: Device.osVersion,
    appVersion: Application.nativeApplicationVersion,
    createdAt: new Date().toISOString(),
  };
  
  // 6. 发送到后端创建账户
  return userProfile;
}
```

### 方案二：设备指纹（更精确）

```javascript
import * as Device from 'expo-device';
import * as Application from 'expo-application';
import * as Localization from 'expo-localization';
import { Platform, Dimensions } from 'react-native';

async function generateDeviceFingerprint() {
  const { width, height } = Dimensions.get('window');
  
  let deviceId;
  if (Platform.OS === 'android') {
    deviceId = Application.androidId;
  } else {
    deviceId = await Application.getIosIdForVendorAsync();
  }
  
  const fingerprint = {
    // 设备标识
    deviceId: deviceId,
    platform: Platform.OS,
    brand: Device.brand,
    model: Device.modelName,
    osVersion: Device.osVersion,
    
    // 屏幕信息
    screenWidth: width,
    screenHeight: height,
    
    // 区域信息
    locale: Localization.locale,
    timezone: Localization.timezone,
    
    // 应用信息
    appVersion: Application.nativeApplicationVersion,
    buildNumber: Application.nativeBuildVersion,
  };
  
  // 生成指纹哈希
  const fingerprintString = JSON.stringify(fingerprint);
  // 可以使用 crypto 库生成 hash
  
  return fingerprint;
}
```

---

## 📝 app.json 配置示例

如果需要使用位置等权限，需要在 `app.json` 中配置：

```json
{
  "expo": {
    "name": "QA App",
    "slug": "qa-app",
    "version": "1.0.0",
    "orientation": "portrait",
    "icon": "./assets/icon.png",
    "userInterfaceStyle": "light",
    "splash": {
      "image": "./assets/splash.png",
      "resizeMode": "contain",
      "backgroundColor": "#ffffff"
    },
    "ios": {
      "supportsTablet": true,
      "bundleIdentifier": "com.yourcompany.qaapp",
      "infoPlist": {
        "NSLocationWhenInUseUsageDescription": "我们需要您的位置来显示附近的问题和活动",
        "NSLocationAlwaysAndWhenInUseUsageDescription": "我们需要您的位置来提供更好的服务"
      }
    },
    "android": {
      "adaptiveIcon": {
        "foregroundImage": "./assets/adaptive-icon.png",
        "backgroundColor": "#ffffff"
      },
      "package": "com.yourcompany.qaapp",
      "permissions": [
        "ACCESS_COARSE_LOCATION",
        "ACCESS_FINE_LOCATION"
      ]
    },
    "plugins": [
      [
        "expo-location",
        {
          "locationAlwaysAndWhenInUsePermission": "允许 $(PRODUCT_NAME) 使用您的位置。"
        }
      ]
    ]
  }
}
```

---

## ⚠️ 隐私和合规建议

### 1. 数据收集透明度
- 在首次启动时显示隐私政策
- 明确告知用户收集哪些数据
- 提供选择退出的选项

### 2. 最小化数据收集
- 只收集必要的信息
- 避免收集敏感个人信息
- 定期清理不需要的数据

### 3. 数据安全
- 使用 HTTPS 传输数据
- 敏感信息使用 SecureStore 加密存储
- 不在日志中记录用户隐私信息

### 4. 合规要求
- **GDPR**（欧盟）：需要明确同意
- **CCPA**（加州）：提供数据删除选项
- **PIPL**（中国）：个人信息保护法合规
- **App Store**：遵守苹果隐私政策
- **Google Play**：遵守谷歌隐私政策

---

## 📊 总结对比

| 信息类型 | Android | iOS | 需要权限 | 推荐使用 |
|---------|---------|-----|---------|---------|
| 设备 ID | ✅ | ✅ | ❌ | ✅ |
| 设备型号 | ✅ | ✅ | ❌ | ✅ |
| 系统版本 | ✅ | ✅ | ❌ | ✅ |
| 设备名称 | ✅ | ✅ | ❌ | ⚠️ |
| 位置信息 | ✅ | ✅ | ✅ | ⚠️ |
| 网络状态 | ✅ | ✅ | ❌ | ✅ |
| IP 地址 | ✅ | ✅ | ❌ | ✅ |
| 语言设置 | ✅ | ✅ | ❌ | ✅ |
| 时区 | ✅ | ✅ | ❌ | ✅ |
| 本地存储 | ✅ | ✅ | ❌ | ✅ |

**图例：**
- ✅ 推荐使用
- ⚠️ 谨慎使用（涉及隐私）
- ❌ 不推荐

---

## 🚀 实施建议

1. **优先使用设备 ID + 本地存储方案**
   - 简单可靠
   - 无需额外权限
   - 用户体验好

2. **可选收集位置信息**
   - 用于显示附近内容
   - 必须明确告知用户
   - 提供拒绝选项

3. **收集基础设备信息**
   - 用于统计分析
   - 优化应用性能
   - 提供更好的用户体验

4. **后端生成完整账户**
   - 前端发送设备指纹
   - 后端生成用户 ID
   - 返回 token 给前端
   - 前端保存 token 到 SecureStore

---

**文档版本：** 1.0  
**更新日期：** 2026-02-05  
**适用版本：** Expo SDK 54+
